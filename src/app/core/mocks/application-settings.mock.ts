import { ApplicationSettings, Theme } from "../models";

export const mockApplicationSettings: ApplicationSettings = {
  selectedTheme: Theme.LIGHT,
  notificationCategoryId: 1,
  notificationIntervalInMs: 10000000
}
